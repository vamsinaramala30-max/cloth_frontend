/**
 * Bridge config.
 *
 * Some tooling expects postcss.config.js, while this project uses postcss.config.cjs.
 * Keep this file minimal to avoid changing build behavior.
 */
module.exports = require('./postcss.config.cjs');

